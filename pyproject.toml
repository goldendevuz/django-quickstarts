[project]
name = "django-quickstarts"
version = "0.1.0"
description = "Django quickstarts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2",
    "django-managed-commands>=0.4.0",
]
